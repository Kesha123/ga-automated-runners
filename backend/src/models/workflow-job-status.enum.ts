enum WorkflowJobStatus {
  queued = 'queued',
  in_progress = 'in_progress',
  completed = 'completed',
  waiting = 'waiting',
  requested = 'requested',
  pending = 'pending',
}

export default WorkflowJobStatus;
